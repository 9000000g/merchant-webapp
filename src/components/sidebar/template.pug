fv-sidebar.sidebar(ref='sidebar', position='right', :pin='null', :width='190')
  fv-main
    fv-header.user-dashboard-header
      .fv-icon
      .fv-title
        h3
          i.fa.fa-user.user-dashboard-icon
          |  {{$store.state.me.username}}
    fv-content.no-padding
      fv-list.no-outline-border(v-on:click="go($event)", :items='items')